{
  "name": "portfolio",
  "private": true,
  "scripts": {
    "dev": "live-server --port=8080 --open=/index.html",
    "slides": "live-server --port=8080 --open=/slides/index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
